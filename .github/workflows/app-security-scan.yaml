name: Security Scan

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: read
  pull-requests: write   # required for ggshield-action to comment on PRs

jobs:
  gitguardian-scan:
    name: Scan for Secrets (Report Only)
    runs-on: ubuntu-latest
    continue-on-error: true   # never fail the pipeline, even if ggshield exits non-zero

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0   # full history so multiple commits can be scanned

      - name: GitGuardian scan (GitHub annotation)
        id: ggshield
        uses: GitGuardian/ggshield-action@v1
        continue-on-error: true   # belt-and-suspenders: step itself won't fail the job
        env:
          GITHUB_PUSH_BEFORE_SHA: ${{ github.event.before }}
          GITHUB_PUSH_BASE_SHA: ${{ github.event.base }}
          GITHUB_DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          GITGUARDIAN_API_KEY: ${{ secrets.GITGUARDIAN_API_KEY }}
        with:
          args: --all-secrets   # show known/ignored fixtures too, for full visibility

      - name: Install python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install ggshield (for JSON report)
        if: always()
        run: pip install ggshield --break-system-packages

      - name: Generate JSON findings report
        if: always()
        id: report
        continue-on-error: true
        env:
          GITGUARDIAN_API_KEY: ${{ secrets.GITGUARDIAN_API_KEY }}
        run: |
          ggshield secret scan repo . --all-secrets --json > gitguardian-findings.json || true
          echo "Report saved to gitguardian-findings.json"
          cat gitguardian-findings.json
 
      # ─────────────────────────────────────────────────────────
      # Schema-independent check: a clean scan produces an empty
      # array "[]" (or near-empty content). Anything else means
      # findings exist. Avoids depending on ggshield's exact JSON
      # field names, which may change between versions.
      # ─────────────────────────────────────────────────────────
      - name: Check if findings exist
        if: always()
        id: check
        run: |
          if [ -s gitguardian-findings.json ] && [ "$(tr -d '[:space:]' < gitguardian-findings.json)" != "[]" ]; then
            echo "has_findings=true" >> "$GITHUB_OUTPUT"
            echo "Findings detected — artifact will be uploaded."
          else
            echo "has_findings=false" >> "$GITHUB_OUTPUT"
            echo "No findings — skipping artifact upload."
          fi
 
      - name: Upload findings artifact
        if: always() && steps.check.outputs.has_findings == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: gitguardian-findings-${{ github.run_id }}
          path: gitguardian-findings.json
          retention-days: 30
          if-no-files-found: warn
 
      - name: Summarize outcome (non-blocking)
        if: always()
        run: |
          echo "GitGuardian annotation step outcome: ${{ steps.ggshield.outcome }}"
          echo "Findings present: ${{ steps.check.outputs.has_findings }}"
          echo "This workflow is report-only and does not block merges or deployments."