apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  POSTGRES_PASSWORD: {{ .Values.password | b64enc }}
  POSTGRES_USER: {{ .Values.username | b64enc }}
  POSTGRES_DB: {{ .Values.database | b64enc }}
